services:
  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_URLS: http://+:8080
    depends_on:
      - db
    volumes:
      - ./api/LogiLearn/featureflags.json:/app/featureflags.json
